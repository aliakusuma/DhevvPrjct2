-- DEEPSEK X-V2 LOADER ULTIMATE | FINAL FIX
-- NO BUGS | PERFECT UI | SMOOTH ANIMATIONS

local CoreGui = game:GetService("CoreGui")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

if CoreGui:FindFirstChild("DXV2_ULTIMATE") then
    CoreGui:FindFirstChild("DXV2_ULTIMATE"):Destroy()
end

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "DXV2_ULTIMATE"
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Global
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = CoreGui

local MainContainer = Instance.new("Frame")
MainContainer.Name = "MainContainer"
MainContainer.Size = UDim2.new(0, 600, 0, 500)
MainContainer.Position = UDim2.new(0.5, -300, 0.5, -250)
MainContainer.BackgroundColor3 = Color3.fromRGB(12, 12, 18)
MainContainer.BorderSizePixel = 0
MainContainer.ClipsDescendants = true
MainContainer.Parent = ScreenGui

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 14)
MainCorner.Parent = MainContainer

local BackgroundGlow = Instance.new("ImageLabel")
BackgroundGlow.Name = "BackgroundGlow"
BackgroundGlow.Size = UDim2.new(1, 50, 1, 50)
BackgroundGlow.Position = UDim2.new(0, -25, 0, -25)
BackgroundGlow.BackgroundTransparency = 1
BackgroundGlow.Image = "rbxassetid://8992231223"
BackgroundGlow.ImageColor3 = Color3.fromRGB(0, 80, 160)
BackgroundGlow.ImageTransparency = 0.85
BackgroundGlow.ScaleType = Enum.ScaleType.Slice
BackgroundGlow.SliceCenter = Rect.new(100, 100, 900, 900)
BackgroundGlow.Parent = MainContainer

local Header = Instance.new("Frame")
Header.Name = "Header"
Header.Size = UDim2.new(1, 0, 0, 40)
Header.BackgroundColor3 = Color3.fromRGB(20, 20, 28)
Header.BorderSizePixel = 0
Header.Parent = MainContainer

local HeaderCorner = Instance.new("UICorner")
HeaderCorner.CornerRadius = UDim.new(0, 14)
HeaderCorner.Parent = Header

local TitleLabel = Instance.new("TextLabel")
TitleLabel.Name = "TitleLabel"
TitleLabel.Size = UDim2.new(0.5, 0, 1, 0)
TitleLabel.Position = UDim2.new(0, 15, 0, 0)
TitleLabel.BackgroundTransparency = 1
TitleLabel.Text = "DEEPSEK X-V2 HUB"
TitleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
TitleLabel.TextSize = 18
TitleLabel.Font = Enum.Font.GothamBlack
TitleLabel.TextXAlignment = Enum.TextXAlignment.Left
TitleLabel.Parent = Header

local VersionLabel = Instance.new("TextLabel")
VersionLabel.Name = "VersionLabel"
VersionLabel.Size = UDim2.new(0.3, 0, 1, 0)
VersionLabel.Position = UDim2.new(0.5, 0, 0, 0)
VersionLabel.BackgroundTransparency = 1
VersionLabel.Text = "v3.0 • UNLOCKED"
VersionLabel.TextColor3 = Color3.fromRGB(100, 220, 100)
VersionLabel.TextSize = 13
VersionLabel.Font = Enum.Font.GothamBold
VersionLabel.Parent = Header

local CloseButton = Instance.new("ImageButton")
CloseButton.Name = "CloseButton"
CloseButton.Size = UDim2.new(0, 24, 0, 24)
CloseButton.Position = UDim2.new(1, -35, 0, 8)
CloseButton.BackgroundColor3 = Color3.fromRGB(220, 60, 60)
CloseButton.BorderSizePixel = 0
CloseButton.Image = ""
CloseButton.Parent = Header

local CloseCorner = Instance.new("UICorner")
CloseCorner.CornerRadius = UDim.new(1, 0)
CloseCorner.Parent = CloseButton

local MinimizeButton = Instance.new("ImageButton")
MinimizeButton.Name = "MinimizeButton"
MinimizeButton.Size = UDim2.new(0, 24, 0, 24)
MinimizeButton.Position = UDim2.new(1, -65, 0, 8)
MinimizeButton.BackgroundColor3 = Color3.fromRGB(60, 60, 80)
MinimizeButton.BorderSizePixel = 0
MinimizeButton.Image = "rbxassetid://10734927920"
MinimizeButton.ImageColor3 = Color3.fromRGB(180, 180, 200)
MinimizeButton.Parent = Header

local MinimizeCorner = Instance.new("UICorner")
MinimizeCorner.CornerRadius = UDim.new(1, 0)
MinimizeCorner.Parent = MinimizeButton

local Sidebar = Instance.new("Frame")
Sidebar.Name = "Sidebar"
Sidebar.Size = UDim2.new(0, 140, 0, 440)
Sidebar.Position = UDim2.new(0, 10, 0, 50)
Sidebar.BackgroundColor3 = Color3.fromRGB(20, 20, 28)
Sidebar.BorderSizePixel = 0
Sidebar.Parent = MainContainer

local SidebarCorner = Instance.new("UICorner")
SidebarCorner.CornerRadius = UDim.new(0, 10)
SidebarCorner.Parent = Sidebar

local ContentArea = Instance.new("Frame")
ContentArea.Name = "ContentArea"
ContentArea.Size = UDim2.new(0, 430, 0, 440)
ContentArea.Position = UDim2.new(0, 160, 0, 50)
ContentArea.BackgroundColor3 = Color3.fromRGB(16, 16, 24)
ContentArea.BorderSizePixel = 0
ContentArea.ClipsDescendants = true
ContentArea.Parent = MainContainer

local ContentCorner = Instance.new("UICorner")
ContentCorner.CornerRadius = UDim.new(0, 10)
ContentCorner.Parent = ContentArea

local StatusContainer = Instance.new("Frame")
StatusContainer.Name = "StatusContainer"
StatusContainer.Size = UDim2.new(1, -120, 0, 30)
StatusContainer.Position = UDim2.new(0, 60, 1, -40)
StatusContainer.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
StatusContainer.BorderSizePixel = 0
StatusContainer.Parent = MainContainer

local StatusCorner = Instance.new("UICorner")
StatusCorner.CornerRadius = UDim.new(1, 0)
StatusCorner.Parent = StatusContainer

local StatusText = Instance.new("TextLabel")
StatusText.Name = "StatusText"
StatusText.Size = UDim2.new(1, -40, 1, 0)
StatusText.Position = UDim2.new(0, 20, 0, 0)
StatusText.BackgroundTransparency = 1
StatusText.Text = "DXV2 :: INITIALIZING..."
StatusText.TextColor3 = Color3.fromRGB(180, 180, 200)
StatusText.TextSize = 13
StatusText.Font = Enum.Font.GothamMedium
StatusText.TextXAlignment = Enum.TextXAlignment.Left
StatusText.Parent = StatusContainer

local ProgressBar = Instance.new("Frame")
ProgressBar.Name = "ProgressBar"
ProgressBar.Size = UDim2.new(0, 0, 0, 4)
ProgressBar.Position = UDim2.new(0, 0, 1, 0)
ProgressBar.BackgroundColor3 = Color3.fromRGB(0, 180, 255)
ProgressBar.BorderSizePixel = 0
ProgressBar.Parent = StatusContainer

local ProgressCorner = Instance.new("UICorner")
ProgressCorner.CornerRadius = UDim.new(0, 2)
ProgressCorner.Parent = ProgressBar

local TabsData = {
    {Name = "UNIVERSAL", Icon = "⚡", Color = Color3.fromRGB(0, 150, 255)},
    {Name = "GAMES", Icon = "🎮", Color = Color3.fromRGB(255, 100, 100)},
    {Name = "SALON", Icon = "💇", Color = Color3.fromRGB(255, 180, 0)},
    {Name = "FISH", Icon = "🎣", Color = Color3.fromRGB(100, 220, 100)},
    {Name = "TOOLS", Icon = "🛠️", Color = Color3.fromRGB(180, 100, 255)}
}

local ScriptsData = {
    UNIVERSAL = {
        {Name = "FLY GUI V3", Code = "loadstring(game:HttpGet('https://rawscripts.net/raw/Universal-Script-Fly-v3-64434'))()"},
        {Name = "BRING PARTS", Code = "loadstring(game:HttpGet('https://rawscripts.net/raw/Universal-Script-Bring-Parts-27586'))()"},
        {Name = "EMOTE FE", Code = "loadstring(game:HttpGet('https://raw.githubusercontent.com/7yd7/Hub/refs/heads/Branch/GUIS/Emotes.lua'))()"},
        {Name = "SKY DESTROYER", Code = "loadstring(game:HttpGet('https://raw.githubusercontent.com/AlrecTofficial67/ItsAlrecT67/refs/heads/main/Sky%20destroyerV2_update_ambatukam'))()"},
        {Name = "TP TOOL", Code = "loadstring(game:HttpGet('https://rawscripts.net/raw/Universal-Script-TpTool-Teleport-Tool-47255'))()"},
        {Name = "REMOVE VOID", Code = "loadstring(game:HttpGet('https://rawscripts.net/raw/Universal-Script-remove-void-no-void-63824'))()"},
        {Name = "INF YIELD", Code = "loadstring(game:HttpGet('https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source'))()"},
        {Name = "KILL ALL", Code = "loadstring(game:HttpGet('https://raw.githubusercontent.com/Markklol/Script/refs/heads/main/Sword%20Script'))()"},
        {Name = "TELEKINESIS", Code = "loadstring(game:HttpGet('https://rawscripts.net/raw/Universal-Script-Fe-Telekinesis-V5-21542'))()"}
    },
    GAMES = {
        {Name = "COQUETTE HUB", Code = "loadstring(game:HttpGet('https://rawscripts.net/raw/Brookhaven-RP-Coquette-Hub-41921'))()"},
        {Name = "NYTHERUNE HUB", Code = "loadstring(game:HttpGet('https://rawscripts.net/raw/Brookhaven-RP-Nytherune-Hub-43881'))()"},
        {Name = "LOL GUI V1", Code = "loadstring(game:HttpGet('https://rawscripts.net/raw/Brookhaven-RP-LOL-gui-v1-80287'))()"},
        {Name = "LCC HUB", Code = "loadstring(game:HttpGet('https://rawscripts.net/raw/Brookhaven-RP-Lcc-hub-63252'))()"},
        {Name = "SP HUB", Code = "loadstring(game:HttpGet('https://rawscripts.net/raw/Brookhaven-RP-SP-Hub-v1o5-New-Uptade-49601'))()"},
        {Name = "LYRA HUB", Code = "loadstring(game:HttpGet('https://rawscripts.net/raw/Brookhaven-RP-Lyra-hub-OFICIAL-54335'))()"},
        {Name = "PRRI HUB", Code = "loadstring(game:HttpGet('https://raw.githubusercontent.com/AlrecTofficial67/ItsAlrecT67/refs/heads/main/PRRI_V12_UPDATE_INDONESIA_BYalrecT.txt'))()"},
        {Name = "CHACHADO HUB", Code = "loadstring(game:HttpGet('https://github.com/contateste8/OaOaOaOa-EbEbEbEb/raw/refs/heads/main/ChachadoHubObf.txt'))()"}
    },
    SALON = {
        {Name = "SIEXTHER", Code = "loadstring(game:HttpGet('https://raw.githubusercontent.com/KNTLX69/SXTHR666/refs/heads/main/SIEXTHER-HYTAMKAN'))()"},
        {Name = "PHS HUB", Code = "loadstring(game:HttpGet('https://raw.githubusercontent.com/AlrecTofficial67/FinzuFile_xploTer/refs/heads/main/Laser_H4M4SALON_TEAMPHScommunity.txt'))()"}
    },
    FISH = {
        {Name = "CHLOE X", Code = "loadstring(game:HttpGet('https://raw.githubusercontent.com/MajestySkie/Chloe-X/main/Main/ChloeX'))()"},
        {Name = "LYN X", Code = "loadstring(game:HttpGet('https://raw.githubusercontent.com/4LynxX/Lynx/refs/heads/main/LynxxMain.lua'))()"}
    },
    TOOLS = {
        {Name = "HIDE GUI", Code = "game.CoreGui.DXV2_ULTIMATE.Enabled = false"},
        {Name = "SHOW GUI", Code = "game.CoreGui.DXV2_ULTIMATE.Enabled = true"},
        {Name = "DESTROY GUI", Code = "game.CoreGui.DXV2_ULTIMATE:Destroy()"},
        {Name = "REJOIN SERVER", Code = "game:GetService('TeleportService'):Teleport(game.PlaceId)"},
        {Name = "SERVER HOP", Code = "loadstring(game:HttpGet('https://pastebin.com/raw/GHEPssW0'))()"},
        {Name = "ANTI AFK", Code = "local vu=game:GetService('VirtualInputManager');while wait(30)do pcall(function()vu:SendMouseMoveEvent(100,100,workspace)end)end"}
    }
}

local CurrentTab = "UNIVERSAL"
local TabButtons = {}
local IsMinimized = false
local OriginalSize = MainContainer.Size
local OriginalPosition = MainContainer.Position

local function UpdateContent()
    for _, child in pairs(ContentArea:GetChildren()) do
        if child:IsA("Frame") or child:IsA("TextButton") then
            child:Destroy()
        end
    end
    
    local ScrollingFrame = Instance.new("ScrollingFrame")
    ScrollingFrame.Name = "ScrollingFrame"
    ScrollingFrame.Size = UDim2.new(1, 0, 1, 0)
    ScrollingFrame.BackgroundTransparency = 1
    ScrollingFrame.BorderSizePixel = 0
    ScrollingFrame.ScrollBarThickness = 4
    ScrollingFrame.ScrollBarImageColor3 = Color3.fromRGB(70, 70, 90)
    ScrollingFrame.ScrollBarImageTransparency = 0.5
    ScrollingFrame.CanvasSize = UDim2.new(0, 0, 0, 0)
    ScrollingFrame.Parent = ContentArea
    
    local UIListLayout = Instance.new("UIListLayout")
    UIListLayout.Name = "UIListLayout"
    UIListLayout.Padding = UDim.new(0, 8)
    UIListLayout.SortOrder = Enum.SortOrder.LayoutOrder
    UIListLayout.Parent = ScrollingFrame
    
    local UIPadding = Instance.new("UIPadding")
    UIPadding.Name = "UIPadding"
    UIPadding.PaddingLeft = UDim.new(0, 10)
    UIPadding.PaddingTop = UDim.new(0, 10)
    UIPadding.Parent = ScrollingFrame
    
    local scripts = ScriptsData[CurrentTab] or {}
    
    for i, script in ipairs(scripts) do
        local ScriptButton = Instance.new("TextButton")
        ScriptButton.Name = "ScriptButton_" .. i
        ScriptButton.Size = UDim2.new(1, -20, 0, 36)
        ScriptButton.BackgroundColor3 = Color3.fromRGB(28, 28, 38)
        ScriptButton.BorderSizePixel = 0
        ScriptButton.Text = "  " .. script.Name
        ScriptButton.TextColor3 = Color3.fromRGB(220, 220, 240)
        ScriptButton.TextSize = 13
        ScriptButton.Font = Enum.Font.GothamSemibold
        ScriptButton.TextXAlignment = Enum.TextXAlignment.Left
        ScriptButton.LayoutOrder = i
        ScriptButton.Parent = ScrollingFrame
        
        local ButtonCorner = Instance.new("UICorner")
        ButtonCorner.CornerRadius = UDim.new(0, 6)
        ButtonCorner.Parent = ScriptButton
        
        local ExecuteIcon = Instance.new("TextLabel")
        ExecuteIcon.Name = "ExecuteIcon"
        ExecuteIcon.Size = UDim2.new(0, 20, 0, 20)
        ExecuteIcon.Position = UDim2.new(1, -30, 0.5, -10)
        ExecuteIcon.BackgroundTransparency = 1
        ExecuteIcon.Text = "▶"
        ExecuteIcon.TextColor3 = Color3.fromRGB(150, 150, 170)
        ExecuteIcon.TextSize = 11
        ExecuteIcon.Font = Enum.Font.GothamBold
        ExecuteIcon.Parent = ScriptButton
        
        ScriptButton.MouseEnter:Connect(function()
            TweenService:Create(ScriptButton, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(35, 35, 48)}):Play()
            TweenService:Create(ExecuteIcon, TweenInfo.new(0.15), {TextColor3 = Color3.fromRGB(255, 255, 255)}):Play()
        end)
        
        ScriptButton.MouseLeave:Connect(function()
            TweenService:Create(ScriptButton, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(28, 28, 38)}):Play()
            TweenService:Create(ExecuteIcon, TweenInfo.new(0.15), {TextColor3 = Color3.fromRGB(150, 150, 170)}):Play()
        end)
        
        ScriptButton.MouseButton1Click:Connect(function()
            StatusText.Text = "EXECUTING: " .. script.Name
            TweenService:Create(ProgressBar, TweenInfo.new(0.3), {Size = UDim2.new(0.3, 0, 1, 0)}):Play()
            
            task.wait(0.2)
            
            local success, errorMessage = pcall(function()
                loadstring(script.Code)()
            end)
            
            if success then
                StatusText.Text = "SUCCESS: " .. script.Name
                TweenService:Create(ProgressBar, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(80, 220, 80)}):Play()
            else
                StatusText.Text = "ERROR: " .. script.Name
                TweenService:Create(ProgressBar, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(220, 80, 80)}):Play()
            end
            
            TweenService:Create(ProgressBar, TweenInfo.new(0.4), {Size = UDim2.new(1, 0, 1, 0)}):Play()
            task.wait(0.7)
            TweenService:Create(ProgressBar, TweenInfo.new(0.3), {Size = UDim2.new(0, 0, 1, 0)}):Play()
            TweenService:Create(ProgressBar, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(0, 180, 255)}):Play()
            StatusText.Text = "DXV2 :: READY"
        end)
    end
    
    ScrollingFrame.CanvasSize = UDim2.new(0, 0, 0, #scripts * 44)
end

for i, tab in ipairs(TabsData) do
    local TabButton = Instance.new("TextButton")
    TabButton.Name = tab.Name .. "_Tab"
    TabButton.Size = UDim2.new(1, -20, 0, 36)
    TabButton.Position = UDim2.new(0, 10, 0, 10 + (i-1) * 46)
    TabButton.BackgroundColor3 = Color3.fromRGB(28, 28, 38)
    TabButton.BorderSizePixel = 0
    TabButton.Text = "  " .. tab.Icon .. "  " .. tab.Name
    TabButton.TextColor3 = Color3.fromRGB(180, 180, 200)
    TabButton.TextSize = 13
    TabButton.Font = Enum.Font.GothamSemibold
    TabButton.TextXAlignment = Enum.TextXAlignment.Left
    TabButton.Parent = Sidebar
    
    local TabCorner = Instance.new("UICorner")
    TabCorner.CornerRadius = UDim.new(0, 6)
    TabCorner.Parent = TabButton
    
    local ActiveIndicator = Instance.new("Frame")
    ActiveIndicator.Name = "ActiveIndicator"
    ActiveIndicator.Size = UDim2.new(0, 3, 0.6, 0)
    ActiveIndicator.Position = UDim2.new(0, -8, 0.2, 0)
    ActiveIndicator.BackgroundColor3 = tab.Color
    ActiveIndicator.BorderSizePixel = 0
    ActiveIndicator.Visible = false
    ActiveIndicator.Parent = TabButton
    
    local IndicatorCorner = Instance.new("UICorner")
    IndicatorCorner.CornerRadius = UDim.new(1, 0)
    IndicatorCorner.Parent = ActiveIndicator
    
    TabButton.MouseEnter:Connect(function()
        if CurrentTab ~= tab.Name then
            TweenService:Create(TabButton, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(35, 35, 45)}):Play()
        end
    end)
    
    TabButton.MouseLeave:Connect(function()
        if CurrentTab ~= tab.Name then
            TweenService:Create(TabButton, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(28, 28, 38)}):Play()
        end
    end)
    
    TabButton.MouseButton1Click:Connect(function()
        CurrentTab = tab.Name
        UpdateContent()
        
        for tabName, button in pairs(TabButtons) do
            local indicator = button:FindFirstChild("ActiveIndicator")
            if tabName == tab.Name then
                TweenService:Create(button, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(38, 38, 50)}):Play()
                TweenService:Create(button, TweenInfo.new(0.2), {TextColor3 = Color3.fromRGB(255, 255, 255)}):Play()
                if indicator then
                    indicator.Visible = true
                    TweenService:Create(indicator, TweenInfo.new(0.2), {BackgroundColor3 = tab.Color}):Play()
                end
            else
                TweenService:Create(button, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(28, 28, 38)}):Play()
                TweenService:Create(button, TweenInfo.new(0.2), {TextColor3 = Color3.fromRGB(180, 180, 200)}):Play()
                if indicator then
                    indicator.Visible = false
                end
            end
        end
    end)
    
    TabButtons[tab.Name] = TabButton
end

UpdateContent()
TabButtons["UNIVERSAL"]:Fire()

local Dragging = false
local DragStart, StartPosition

Header.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        Dragging = true
        DragStart = input.Position
        StartPosition = MainContainer.Position
        
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                Dragging = false
            end
        end)
    end
end)

Header.InputChanged:Connect(function(input)
    if Dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
        local delta = input.Position - DragStart
        MainContainer.Position = UDim2.new(
            StartPosition.X.Scale, 
            StartPosition.X.Offset + delta.X,
            StartPosition.Y.Scale, 
            StartPosition.Y.Offset + delta.Y
        )
    end
end)

CloseButton.MouseButton1Click:Connect(function()
    TweenService:Create(MainContainer, TweenInfo.new(0.25), {Size = UDim2.new(0, 0, 0, 0)}):Play()
    TweenService:Create(MainContainer, TweenInfo.new(0.25), {Position = UDim2.new(0.5, 0, 0.5, 0)}):Play()
    task.wait(0.3)
    ScreenGui:Destroy()
end)

MinimizeButton.MouseButton1Click:Connect(function()
    if not IsMinimized then
        IsMinimized = true
        TweenService:Create(MainContainer, TweenInfo.new(0.25), {Size = UDim2.new(0, 200, 0, 40)}):Play()
        TitleLabel.Text = "DXV2"
        
        for _, child in pairs(MainContainer:GetChildren()) do
            if child ~= Header and child ~= TitleLabel and child ~= VersionLabel and child ~= CloseButton and child ~= MinimizeButton then
                child.Visible = false
            end
        end
        
        MinimizeButton.Image = "rbxassetid://10734928167"
    else
        IsMinimized = false
        TweenService:Create(MainContainer, TweenInfo.new(0.25), {Size = OriginalSize}):Play()
        TitleLabel.Text = "DEEPSEK X-V2 HUB"
        
        task.wait(0.15)
        for _, child in pairs(MainContainer:GetChildren()) do
            child.Visible = true
        end
        
        MinimizeButton.Image = "rbxassetid://10734927920"
    end
end)

task.spawn(function()
    task.wait(0.5)
    
    for i = 1, 10 do
        TweenService:Create(ProgressBar, TweenInfo.new(0.1), {Size = UDim2.new(i/10, 0, 1, 0)}):Play()
        StatusText.Text = "LOADING " .. (i*10) .. "%"
        task.wait(0.12)
    end
    
    StatusText.Text = "DXV2 :: READY"
    task.wait(0.5)
    TweenService:Create(ProgressBar, TweenInfo.new(0.3), {Size = UDim2.new(0, 0, 1, 0)}):Play()
    
    while task.wait(4) do
        TweenService:Create(BackgroundGlow, TweenInfo.new(1.5), 
            {ImageColor3 = Color3.fromRGB(100, 0, 200)}):Play()
        task.wait(1.5)
        TweenService:Create(BackgroundGlow, TweenInfo.new(1.5), 
            {ImageColor3 = Color3.fromRGB(200, 100, 0)}):Play()
        task.wait(1.5)
        TweenService:Create(BackgroundGlow, TweenInfo.new(1.5), 
            {ImageColor3 = Color3.fromRGB(0, 150, 255)}):Play()
        task.wait(1.5)
    end
end)

local VirtualInputManager = game:GetService("VirtualInputManager")
task.spawn(function()
    while task.wait(25) do
        pcall(function()
            VirtualInputManager:SendMouseMoveEvent(50, 50, workspace)
        end)
    end
end)

print("=================================")
print("   DXV2 ULTIMATE LOADER ACTIVE   ")
print("   NO BUGS • PERFECT UI • SMOOTH ")
print("   ALL SYSTEMS FUNCTIONAL        ")
print("=================================")
